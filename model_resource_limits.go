/*
 * CLOUD API
 *
 * An enterprise-grade Infrastructure is provided as a Service (IaaS) solution that can be managed through a browser-based \"Data Center Designer\" (DCD) tool or via an easy to use API.   The API allows you to perform a variety of management tasks such as spinning up additional servers, adding volumes, adjusting networking, and so forth. It is designed to allow users to leverage the same power and flexibility found within the DCD visual tool. Both tools are consistent with their concepts and lend well to making the experience smooth and intuitive.
 *
 * API version: 5.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package ionossdk
// ResourceLimits struct for ResourceLimits
type ResourceLimits struct {
	// maximum number of cores per server
	CoresPerServer *int32 `json:"coresPerServer,omitempty"` /* nullable: false */
	// maximum cores per contract
	CoresPerContract *int32 `json:"coresPerContract,omitempty"` /* nullable: false */
	// number of cores provisioned
	CoresProvisioned *int32 `json:"coresProvisioned,omitempty"` /* nullable: false */
	// maximum ram per server
	RamPerServer *int32 `json:"ramPerServer,omitempty"` /* nullable: false */
	// maximum ram per contract
	RamPerContract *int32 `json:"ramPerContract,omitempty"` /* nullable: false */
	// ram provisioned
	RamProvisioned *int32 `json:"ramProvisioned,omitempty"` /* nullable: false */
	// hdd limit per volume
	HddLimitPerVolume *int64 `json:"hddLimitPerVolume,omitempty"` /* nullable: false */
	// hdd limit per contract
	HddLimitPerContract *int64 `json:"hddLimitPerContract,omitempty"` /* nullable: false */
	// hdd volume provisioned
	HddVolumeProvisioned *int64 `json:"hddVolumeProvisioned,omitempty"` /* nullable: false */
	// ssd limit per volume
	SsdLimitPerVolume *int64 `json:"ssdLimitPerVolume,omitempty"` /* nullable: false */
	// ssd limit per contract
	SsdLimitPerContract *int64 `json:"ssdLimitPerContract,omitempty"` /* nullable: false */
	// ssd volume provisioned
	SsdVolumeProvisioned *int64 `json:"ssdVolumeProvisioned,omitempty"` /* nullable: false */
	// total reservable ip limit of the customer
	ReservableIps *int32 `json:"reservableIps,omitempty"` /* nullable: false */
	// reserved ips on a contract
	ReservedIpsOnContract *int32 `json:"reservedIpsOnContract,omitempty"` /* nullable: false */
	// reserved ips in use
	ReservedIpsInUse *int32 `json:"reservedIpsInUse,omitempty"` /* nullable: false */
	// k8s clusters total limit
	K8sClusterLimitTotal *int32 `json:"k8sClusterLimitTotal,omitempty"` /* nullable: false */
	// k8s clusters provisioned
	K8sClustersProvisioned *int32 `json:"k8sClustersProvisioned,omitempty"` /* nullable: false */
}
